# ACCEPTANCE_CRITERIA.md

```yaml
core_loop_status: GREEN
active_acceptance_id: A131
green_count: 129
yellow_count: 0
red_count: 0
deferred_count: 2
blocked_count: 0
last_green_ids: ["A01","A02","A03","A04","A05","A06","A07","A08","A09","A10","A11","A12","A14","A15","A16","A17","A18","A19","A21","A22","A23","A24","A25","A26","A27","A28","A29","A30","A31","A32","A33","A34","A35","A36","A37","A38","A39","A40","A41","A42","A43","A44","A45","A46","A47","A48","A49","A50","A51","A52","A53","A54","A55","A56","A57","A58","A59","A60","A61","A62","A63","A64","A65","A66","A67","A68","A69","A70","A71","A72","A73","A74","A75","A76","A77","A78","A79","A80","A81","A82","A83","A84","A85","A86","A87","A88","A89","A90","A91","A92","A93","A94","A95","A96","A97","A98","A99","A100","A101","A102","A103","A104","A105","A106","A107","A108","A109","A110","A111","A112","A113","A114","A115","A116","A117","A118","A119","A120","A121","A122","A123","A124","A125","A126","A127","A128","A129","A130","A131"]
last_yellow_ids: []
last_red_ids: []
last_deferred_ids: ["A13","A20"]
last_blocker_signature: ""
```

> Restoration note: the exact pre-compaction file was not recoverable in this non-git workspace. The compact replacement was preserved as `naive_acceptance.md`; this canonical file was rebuilt into the prior detailed style from `WORKLOG.md` and current evidence.

Status values:

- RED: not started or broken
- YELLOW: partially working
- GREEN: working and validated with proof
- DEFERRED: intentionally out of MVP scope

## Evidence Rule

An acceptance criterion may be marked GREEN only when the Proof column includes at least one of: passed command, passed test, manually verified route, screenshot/artifact path, seed/data evidence, or explicit documented reason for deferral.

## Latest Expansion

| ID | Requirement | Status | Proof |
|---|---|---:|---|
| A131 | Spread Speed uses sidecar-only saved state and cannot reopen the giant upload payload | GREEN | `server\spreadSpeed.ts` now serves through `loadOrBuildSpreadSpeedState()` only. Rebuilds use `loadSafeSpxBars()` and option-leg sidecar CSV candidates, and the module no longer imports the payload-fallback `loadSpxBars()` loader. `server\index.ts` no longer forwards `/api/spread-speed?full=1`; stale full-mode callers stay on the safe-state path. Focused mocked/temp-fixture Spread Speed tests passed 2 files / 4 tests without touching real archive files, typecheck passed, and build passed with only the existing Vite large-chunk warning. |
| A130 | Explicit Replay uses a per-date safe replay state by default | GREEN | `rubicon_replay_safe_state.json` is written beside each date archive and default `/api/replay` reads it before raw artifacts. Daily sync completion refreshes this safe state with tracker and Morning derived state. Safe Replay avoids the 449MB sheet payload, keeps SPX bars, quick-trade spread marks, OI, and five-minute volume profile rows, while `/api/replay?full=1` remains available for raw-detail audit. Focused importer/sync/summary tests, typecheck, build, desktop relaunch, and heavy-date timing probes passed. |
| A129 | Daily sync completion refreshes derived state and Morning auto-refreshes after the 8:30 ET data window | GREEN | `server\dailySync.ts` now refreshes both `rubicon_tracker_summary.json` and saved Morning state after the sync process closes. `src\dailySyncRefresh.ts` keys completed syncs by finish time and latest summary date; `src\App.tsx` uses that key in both status-poll paths so tracker/Daily Pull state refreshes once per observed completion. `src\morningAutoArm.ts` adds an 8:30-or-later once-per-ET-date Morning refresh decision; `src\components\MorningDashboard.tsx` uses it to pull live Morning data and rewrite saved state when Morning is showing today's date. Targeted tests passed 3 files / 19 tests, affected tests passed 5 files / 35 tests, full Vitest passed 46 files / 242 tests, typecheck/build passed, and desktop health/status probes passed. |
| A128 | Morning startup avoids row-level Replay/Spread Speed hydration | GREEN | `/api/tracker` marks replay readiness from compact daily summary fields instead of reading SPX rows from the 449MB `google_sheet_upload_payload.json`, and `src\App.tsx` fetches `/api/replay` or selected-date `/api/spread-speed` only when Replay/Daily Review views need them. Focused importer/summary tests, typecheck, build, desktop relaunch, one-off timing/memory probes, and live `/api/tracker` probes passed. Remaining caveat at the time: explicit Replay was still heavy; A131 later closed the Spread Speed fallback path. |
| A127 | Morning brief reads saved per-date state on normal loads and refreshes live sources only on explicit refresh | GREEN | `server\morningBrief.ts` persists `data\morning-brief-state\YYYY-MM-DD.json` with the dashboard-ready Morning payload and a small schema/version/savedAt wrapper. Normal `/api/morning?date=...` reads that saved state first; `/api/morning?date=...&refresh=1` pulls live DailyFX/RollCall/TC2000 data, rewrites state, and returns a `Morning brief state` source row. `src\components\MorningDashboard.tsx` sends `refresh=1` only from the `Refresh Morning` button, leaving initial loads and polling on saved state. Focused Morning-state tests, Morning server/dashboard tests, full Vitest, typecheck, build, desktop relaunch, and live API probes passed. |
| A126 | Tracker dashboard reads compact validated daily summaries instead of giant row-level artifacts | GREEN | `server\trackerSummary.ts` writes compact `rubicon_tracker_summary.json` files beside daily pull folders, using `daily_sync_summary.json` plus file-existence evidence while leaving giant `google_sheet_upload_payload.json` and row-level IBKR CSVs for replay/audit/detail views. `server\dataImporter.ts` prefers compact summaries, coalesces/caches immediate tracker reads, prevents stale connector rows from downgrading validated local IBKR facts, and keeps replay mark hydration out of `/api/tracker`. `server\dailySync.ts` refreshes the compact summary after completion. Focused backend tests passed 3 files / 40 tests, typecheck and build passed, and one-off loader proof returned June 1 option status `partial`, 540,996 option/volume rows, upload status `uploaded`, and about 1.2s tracker load time. |
| A125 | Godel bottom-right blips can be captured while the Godel window is minimized | GREEN | Live Updates renders a minimized-safe Godel DOM bridge setup/status strip. The setup page provides a bookmarklet that arms a MutationObserver inside the Godel page, watches bottom-right/toast-like DOM changes, posts headline-like blips back to Rubicon, and rejects numeric ladder rows. Focused bridge/Morning/Godel tests, typecheck, build, and live setup/status/ingest/API probes passed. |
| A124 | Godel alert watcher can be started/stopped from Morning and rejects numeric false positives | GREEN | Live Updates renders visible-screen watcher controls with Start/Stop/status. The watcher calibrates from a visible Godel version anchor to the right edge above the bottom bar, reports `anchor-not-found` when Godel is not visible, and rejects mostly numeric ladder rows. Focused Python watcher checks, Morning/Godel tests, typecheck, build, desktop relaunch, and API probes passed. |
| A123 | Desktop launcher restarts stale Rubicon backends before opening | GREEN | `scripts\launch-desktop.mjs` now rebuilds stale `dist`, restarts existing Rubicon servers by `/api/health` PID, cleans up detached non-watch `server/index.ts` Rubicon processes from this app root including stale ports like `5187`, scans `5187`, and starts the desktop API with configurable `NODE_OPTIONS=--max-old-space-size=16384` by default. `/api/health` now returns `pid`, `startedAt`, and `appRoot`. Runtime proof after `npm run desktop` returned fresh health on `http://[::1]:5174` and `/api/morning?date=2026-06-08` returned `inflationExpectationsCount=0`. Syntax check, typecheck, and build passed. |
| A122 | Daily Review counts regular exits by closing action side | GREEN | CCS exits now map to Long/Put action side and PCS exits map to Short/Call action side in `buildDailyReview()` and `buildReviewMarkers()`. Timeline pills show Long/Short, the chart legend explains Long = PCS entries / CCS exits and Short = CCS entries / PCS exits, focused `src/stats.test.ts` passed 24 tests, typecheck passed, and build passed. |
| A121 | Morning high-importance events match live DailyFX/IG rows and preserve source names | GREEN | Live DailyFX/IG endpoint probe for `2026-06-01` through `2026-06-15` returned only U.S. `importance: 3` rows; direct `loadMorningBrief("2026-06-02")` matched those rows with no extras. `clusterMajorMacroEvents()` now preserves exact source event titles for single-row events and source-name cluster titles for simultaneous rows, while the panel says `High-importance events`. Focused parser/component tests, typecheck, and build passed. |
| A120 | Morning Calendar splits today's agenda left and High-only major events right | GREEN | App proof: `src\components\MorningDashboard.tsx` renders `.morning-calendar-body` with today's event list first and Major events second. CSS splits that body into equal desktop columns and stacks it on narrow viewports. Parser proof: `server\morningBrief.ts` filters Major events to DailyFX/IG rows with `importance >= 3` while keeping native monthly OPEX markers. Browser proof at `http://[::1]:5174/?qa=calendar-split-high-only`: equal desktop columns, Major events right of today's agenda, no medium major text, clean warning/error logs, and no mobile overflow. Validation: focused tests, typecheck, and build passed; full MVP validation still has unrelated importer data-drift failures. |
| A119 | Morning live-update word-filter matches trigger Windows desktop popups | GREEN | Existing-feature check: Morning parsed/deduped live-update filter terms, highlighted matching rows, and beeped for new matches, but only calendar alerts called the Windows popup helper. App proof: `src\liveUpdateAlerts.ts` builds a readable desktop alert payload from the first new matching update, includes matched terms plus source/time/count detail, and dispatches one grouped popup per matching refresh batch. `MorningDashboard` calls that dispatcher beside the existing beep path through the same local desktop alert endpoint as calendar alerts. Validation: focused tests passed 3 files / 12 tests, typecheck and build passed, Browser smoke showed word-filter and alert controls with clean console. |
| A118 | Morning calendar refresh does not wait on failed live-update feeds | GREEN | Root cause proof: full Morning refresh waited on FirstSquawk/Godel live sources; FirstSquawk was returning `fetch failed`, causing long Nitter timeline/RSS fallback windows. App proof: `loadMorningBrief()` now loads DailyFX, major events, RollCall, TC2000, and cached live tape only; `/api/morning/live-updates` performs FirstSquawk/Godel pulls separately and the UI prevents overlapping 10-second live requests. Timing proof: `/api/morning` dropped from about 21.8s to about 1.7s during the current Nitter failure. |
| A117 | Replay left date tabs hide May 26 and May 27 without deleting data | GREEN | `visibleReplayDateTabs()` hides `2026-05-26` and `2026-05-27` from display-only Replay rails. Data proof: `/api/tracker` still returns both dates. Focused test, typecheck, build, Browser proof, and API data check passed. |
| A116 | Journal makes the four spread-entry aspects easy to mark per trade | GREEN | Journal entries persist `aspectChecks` for entry structure, price action, option-volume-node placement, and optional orderflow. Credit call spreads render lower-high / above-OVN / selling-flow wording; credit put spreads render higher-low / below-OVN / buying-flow wording. UI proof showed required-check counters and queue progress pills. Focused journal/snapshot tests, typecheck, and build passed. |
| A115 | Replay and review date-rail issue badges reflect red Required Outputs rows only | GREEN | Date badges now build from Required Outputs coverage rows and count only failed rows, so warning-only raw diagnostics do not create issue badges when usability-critical outputs are green. Focused badge/checklist tests, runtime API proof, typecheck, and build passed. |
| A114 | Morning Calendar shows a this-week/next-week major-events outlook without Today/Tomorrow shortcuts | GREEN | Morning no longer renders Today/Tomorrow shortcuts. Calendar includes `Major events` with `This week` and `Next week` groups, clustered from high/major US DailyFX/IG macro rows plus native monthly OPEX markers. Focused tests, API smoke, Browser proof, typecheck, and build passed. |
| A113 | Morning calendar dates use Eastern today/tomorrow instead of Replay's latest trade date | GREEN | Morning has a separate `morningDate`, initialized to Eastern today and rolling forward while tracking Today. Signal Stack fetches spread speed for Morning date instead of Replay date. Focused date/brief tests, Browser proof, typecheck, and build passed. |
| A112 | Morning source refresh keeps live tape state and restores DailyFX/RollCall calendar pulls | GREEN | DailyFX now reads the IG economic events endpoint; RollCall uses month-specific Factba.se pages; live updates persist/read `data/morning-live-updates-cache.json` as a server-side last-good fallback. Full `npm run validate:mvp` passed at the time of the run. |
| A111 | Morning FirstSquawk updates read naturally and use the freshest available local source | GREEN | FirstSquawk rows render through `formatLiveUpdateDisplayText()`, preserving acronyms while normalizing shouted wire copy. Source tries Nitter timeline before RSS and surfaces freshness/TTL details. Focused tests, Browser proof, typecheck, and build passed. |
| A110 | Morning Live Updates refresh gives visible proof when FirstSquawk returns the same cached rows | GREEN | Manual refresh uses cache-busting params, no-store API headers, no-cache feed headers, and UI status with checked time, new-post count, source counts, and newest-item age. API and Browser proof showed refresh state changed even when upstream rows were cached. |
| A109 | FPL Indicator live chart backfills the current session after a midday predictor restart | GREEN | Live predictor loads existing rows, dedupes/sorts by ET minute, seeds completed bars from IBKR snapshot data, recomputes partial rows, and streams new completed bars. API proof returned a full intraday bar set after restart. Python compile/helper smoke and typecheck passed. |
| A108 | FPL Indicator live prediction recovers from an already-running live predictor and renders today live | GREEN | Server detects existing Python live predictor processes, avoids duplicate IBKR client-id starts, can stop them, and defaults fresh starts to client id 177. UI polls live predictions while running. Full MVP validation passed at the time of the run. |
| A107 | Morning IBKR holdings label option delta as decimal per contract and share-equivalent delta separately | GREEN | Holding rows render per-contract option delta, such as `Delta 0.53/ct`, and converted share-equivalent exposure separately, such as `Share eq +791`. Typecheck, build, holdings tests, and Browser proof passed. |
| A106 | IBKR holdings pull uses native Greeks when available and manually computes missing option delta/theta each pull | GREEN | Holdings refresh uses IBKR model Greeks when available and Black-Scholes fallback when missing. Live proof returned market prices, deltas, and thetas for current option positions; forced fallback computed Greeks manually. Full MVP validation passed at the time of the run. |
| A105 | Morning tape readability, TC2000 profile hover blurbs, and IBKR option earnings warnings are generated from refresh paths | GREEN | Live Updates text is lighter/readable, TC2000 hover cards show daily-generated industry/company profiles, and IBKR option positions can carry earnings warning metadata. Full MVP validation passed at the time of the run. |
| A104 | Morning Live Updates can be manually refreshed, Godel is visible as a source, and TC2000 includes Jump Pause v2 with reliable hover previews | GREEN | Live Updates has `Refresh Live`, FirstSquawk/Godel source counts, and source diagnostics. TC2000 includes Jump Pause v2 and daily-bar hover previews. Full MVP validation passed at the time of the run. |
| A103 | Morning live updates do not clear on transient refreshes, and TC2000 hits have Staircase plus hover daily charts | GREEN | `src\morningLiveState.ts` preserves last non-empty live updates through transient empty refreshes. TC2000 merges Staircase and Three Bar scanner lists and sync wrapper refreshes daily bars. Full MVP validation passed at the time of the run. |
| A102 | Morning live filters are fast/case-insensitive, Godel has a real capture path, and IBKR holdings show option premium/Greek context | GREEN | Live filters compile once, match case-insensitively, and highlight rows. Godel parser/capture path can read configured staged sources. IBKR holdings display premium-style option prices and Greek context. Full MVP validation passed at the time of the run. |
| A101 | Morning live tape shows reposts/freshness accurately, AI notes retry transient failures, and IBKR holdings stay dense | GREEN | FirstSquawk RSS parser labels repost/reply metadata and exposes newest-item age/TTL; AI notes automation validates and retries transient failures; holdings panel is denser. Full MVP validation passed at the time of the run. |
| A100 | Morning AI notes are automation-backed, live-update alarms require filter matches, and Morning review panels stay compact/text-first | GREEN | AI notes read automation output from disk; journal snapshots feed the automation; empty live filters stay silent; TC2000 renders text/hit lists rather than screenshots; IBKR holdings are compact. Full MVP validation passed at the time of the run. |
| A99 | Morning auto-arms alerts, filters live-update beeps, polls tape every 10 seconds, and shows live IBKR holdings | GREEN | Morning starts with live and calendar alerts armed, re-arms on weekday mornings, polls live updates every 10 seconds, filters beeps by configured terms, and can pull/read live IBKR holdings. Full MVP validation passed at the time of the run. |
| A98 | Morning calendar events can alert 1 minute before start with sound and a smaller-monitor popup | GREEN | Calendar alerts schedule timed events 60 seconds before start, play sound, show in-app fallback, and call `/api/desktop-alert/calendar` to launch a topmost Windows popup on the smallest available monitor. Full MVP validation passed at the time of the run. |
| A97 | Morning Brief shows a single chronological DailyFX + presidential agenda with actionable RollCall filtering | GREEN | Brief renders one combined economic/presidential agenda, while RollCall parser filters to actionable official schedule rows and excludes routine/logistics rows. Full MVP validation passed at the time of the run. |
| A96 | Trader can use Rubicon as a two-portion Morning/Replay desktop app | GREEN | App identity changed to Rubicon, top tabs split Morning and Replay, Morning has Brief and Signal Stack sub-screens, and the desktop launcher opens identity-checked Rubicon on the correct local server. Full MVP validation passed at the time of the run. |

## Phase Summary

The local MVP is GREEN through A131. Earlier A01-A95 criteria cover scaffold, import/replay, daily review, source health, Google/IBKR refresh paths, review charts, P/L overlay, journal workflow, Daily Pull diagnostics, and completion audit proof. Detailed run-by-run evidence is preserved in `WORKLOG.md`.

## Deferred

| ID | Requirement | Status | Reason |
|---|---|---:|---|
| A13 | Separate admin/operator view | DEFERRED | Outside local MVP scope. |
| A20 | Full AI feature fallback | DEFERRED | Future scope unless the user makes it core product behavior. |
