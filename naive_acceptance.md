# naive_acceptance.md

```yaml
core_loop_status: GREEN
active_acceptance_id: A186
green_count: 184
green_id_summary: "A01-A12, A14-A19, A21-A186"
yellow_count: 0
red_count: 0
deferred_count: 2
deferred_ids: ["A13", "A20"]
blocked_count: 0
```

## Use This File

- Keep this file short. Put detailed run history in `WORKLOG.md`.
- Add only the current acceptance slice plus the last few relevant deltas.
- A criterion is GREEN only with command, browser, API, data, or artifact proof.

## Current Acceptance

| ID | Requirement | Status | Proof |
|---|---|---:|---|
| A186 | Godel Terminal bottom-corner news is scraped windowlessly to local JSONL (`Desktop\AI STUFF\godel-news\`) | GREEN | Cloudflare blocks ALL headless and raw-HTTP routes (marketing + app + api domains; cleared-profile headless reuse also re-challenged). Working architecture: `scripts/godel-news-scraper.mjs` runs a real headed Edge parked OFF-SCREEN (-32000,-32000, dedicated profile `godel-news/profile`) which passes the managed challenge automatically (fresh profile: app loaded in 1.5s; Turnstile click fallback + challenge-debug screenshot for escalations; a leaked-browser bug that locked the profile was fixed with close-on-error). Captures: (1) DOM poll (5s) of `tr[id*=streaming-table]` rows -> news.jsonl (id/headline/time/symbol/source; layout-defensive date+time cell join) + latest.json (newest 50); (2) red bottom breaking banner + [class*=breaking] -> breaking.jsonl; (3) raw godelterminal WS frames (incl. STOMP /events) -> ws-raw.log (5MB cap). Restart-safe dedupe, crash-relaunch loop; `--once` smoke captured 200 rows incl. same-hour items; `--login` mode for optional account sign-in (persists layout/auth in the profile). Lint clean. Future: `api.godelterminal.com/api/news/items` + `fetchBreaking` (403 logged-out) sit behind the CF cookie. |
| A185 | TC2000 scanner export self-recovers from known blocking prompts and Daily Pull no longer presents stale TC2000 scanner membership as fresh | GREEN | OCR now reports `failureReason: blocking_prompt` / exit 4 for known TC2000 modal text; the control panel dismisses known prompts and retries OCR; the wrapper passes `--fresh-after` + `--require-fresh-sources` to the daily-bars refresh; the bar snapshot carries freshness metadata and Morning marks stale scanner sources as warning. Validation: OCR unittest 5/5, daily-bars unittest 11/11, PowerShell parser checks for TC2000/control/wrapper scripts, focused Vitest 21/21, `npm run typecheck`, full `npm test` 92 files / 584 tests, and `npm run build` passed with the existing large-chunk warning. |
| A184 | Code-quality pass merged from PR #1 (authored on the branch as "A179", renumbered here): lint debt 73→**0** with lint added to `validate:mvp` + CI; component files export only components; React hook-correctness fixes; zero eslint-disables added | GREEN | Branch validation (worktree `rubicon-refactor`, frozen at c84deb6): `npx eslint .` exit 0 repo-wide; underscore unused-vars convention in eslint config; dead assignments/`{cause}`/control-regex/type-only mechanical server fixes; pure logic moved verbatim into `marketChartMarkers.ts`/`replayChartsData.ts`/`reviewEntryExitChartLogic.ts`/`estimatorClock.ts` with all importers updated (`MarketChart.test.ts`→`marketChartMarkers.test.ts`); real bugs fixed: EstimatorSpxChart refs-during-render write deleted, SpxHeatmapPanel unstable `tf` memoized (un-skips React Compiler), one dead App.tsx effect deleted; 15 set-state-in-effect→render-time prev-guards with per-case equivalence reasoning; 565/565 tests, full-surface browser smoke 0 errors/0 overflow, CI green on the PR (2m33s). MERGE (2026-06-11): `dailySync.ts` + `App.tsx` auto-merged (branch effect refactors + main's A179-A183 launcher/self-update landed on disjoint regions); ledger conflicts resolved by renumbering this row A179→A184; post-merge full gates re-run on the merged tree (see WORKLOG). |
| A183 | Self-update gate hardening: an update can never restart the server while a daily sync is mid-pull (hard refusal, force does NOT override — the A181 launcher is attached, so a restart would kill the wrapper), and runtime `data/` churn (e.g. heatmap-classification-auto.json, rewritten continuously by the live app) no longer counts as blocking dirty state | GREEN | `activeDailySyncRunId()` reads the sibling IBKR `daily_sync.lock.json` and probes the pid (`process.kill(pid, 0)`); a live holder puts `syncRunId` into `/api/app-version` and the gate refuses first, before all other checks. `filterUpdateBlockingDirtyFiles` drops `data/`-prefixed paths (both slash shapes) from the dirty list so only source changes block. Unit tests: sync-running hard refusal with force=true, data-churn filter keeps `server/dailySync.ts`/`WORKLOG.md` blocking; selfUpdate suite 9/9 + AppUpdateButton 4/4; typecheck + lint clean. |
| A182 | The header "Latest" button is a real GitHub self-updater: when local main is behind origin/main it offers "Update (n)", and on confirm the server pulls --ff-only, rebuilds, and relaunches itself via the "Rubicon Server" scheduled task — guarded so it never bulldozes local work or kills live feeds silently | GREEN | New `server/selfUpdate.ts`: `GET /api/app-version` (git fetch + rev compare + tracked-dirty list + ET market-hours flag; `?refresh=0` for restart polling) and `POST /api/app-update`. `evaluateUpdateGate` refuses, in order: uncommitted TRACKED changes (untracked ignored), local commits not on GitHub, already up to date, and market hours without explicit force (restart orphans the day's live feeds — they only re-arm in the 09:28 open window; the client confirm carries that warning and sends force). Update path: pull --ff-only → `npm ci` only if package-lock changed → `npm run build` BEFORE restart (build failure leaves the old server untouched) → detached PowerShell relauncher (`Wait-Process` on our pid → `schtasks /Run /TN 'Rubicon Server'`, the supervised rebuild-stale-dist path) → exit. Steps logged to `data/app-update.log`. Client `AppUpdateButton` replaces the static button: amber "Update (n)" when actionable, blocked states explain themselves in the tooltip and fall back to the old bundle-refresh, after POST it polls `?refresh=0` until the revision changes then hard-reloads onto the new bundle. Validation: selfUpdate unit tests 7/7 (porcelain parsing, gate matrix incl. force, RTH boundaries, relauncher args), AppUpdateButton component tests 4/4, App.test 16/16, typecheck/build clean, new files lint-clean; scratch :5191 against the REAL repo state: version endpoint reported behind 0 / dirty 10 / marketHours true and `POST /api/app-update` returned **409 guarded refusal** (dirty tree), header renders "Latest" + honest tooltip, 0 console errors. (Suite note: dailySync's machine-local dry-run preflight test was transiently red during validation — the 12:46 ET real sync holds `daily_sync.lock.json`, so the preflight correctly refuses; unrelated to this change.) Live server serves the new ROUTES after its next restart; until then the button degrades to bundle-refresh with a "version check unavailable" tooltip. |
| A181 | Full Daily Pull monitors the actual wrapper process, so the Pull button no longer reports `exited with code 0` before Data Collection starts | GREEN | The 2026-06-11 12:36 ET run disproved the previous encoded/detached launcher: it exited in ~70ms, wrote no wrapper banner, created no `2026-06-11` trade folder, and left no collector process. `server/dailySync.ts` now spawns the real PowerShell wrapper command with `detached: false` and stdout/stderr file handles to `data\daily-sync-launch.log`; the close handler now belongs to the wrapper, not a parent shell. Regression adds a Windows smoke proving a fake sleeping PowerShell command waits and writes UTF-8 log output. Validation: focused launcher test 22/22, focused sync tests 34/34, typecheck, full `npm test` 90 files / 568 tests, build, server restart, and API dry-run passed. |
| A180 | Review-critical SPX option repair uses the IBKR timeout/chunk pattern we found: bounded spread-leg retry asks smaller chunks first with a 25s request timeout and a larger repair budget, while broad option breadth remains non-blocking after upload | GREEN | `daily_spx_ibkr_sync.py` adds `--option-history-window-mode full-first\|chunk-first`; default remains full-first, but the wrapper's bounded `spx-spread-legs` repair passes `--option-history-window-mode chunk-first`, `--option-request-timeout-s 25`, and a 480s/540s soft/hard budget. Sidecar summaries now record `history_window_mode`. Regression: Python unit asserts chunk-first orders `chunk_01..chunk_04` before `full_session`; wrapper Vitest asserts the SPX spread-leg repair uses chunk-first + 25s + the longer budget. Validation: IBKR Python tests 30/30, PowerShell AST parser OK, focused Daily Sync tests 33/33, full Rubicon tests 90 files / 567 tests, typecheck, and build passed. |
| A179 | Full Daily Pull launch no longer relies on the original detached numeric-fd stdio path | GREEN | Superseded by A181 after the encoded/detached follow-up still reproduced the immediate code-0/no-banner failure on 2026-06-11. The accepted implementation is now direct monitored wrapper spawn with file-handle logging. |
| A178 | The ENLARGED replay chart is as clean as the grid view — compact ticks only (scaled up), with trade/time detail coming from hover like the Daily Review chart; the printed-label arrow mode is retired from the replay cockpit | GREEN | Follow-up to A175/A177 per user feedback ("enlarged screen does not need to look like this... it should be like daily review where that information comes from hovering"). `ReplayCharts` now passes `markerMode="compact"` in EVERY state (all three chart variants); `createEventTick` is scale-aware (stub width 3×scale / height 11×scale, hairline repositioned), so the enlarged view shows slightly larger ticks on an otherwise unobstructed full-viewport chart. Hover `title` per tick carries the detail (cluster-deduped, e.g. "Entry 09:30", "X2-3 11:17"). The full arrow/label pipeline remains in `MarketChart` (tested, unused by the cockpit) for potential other surfaces. Tests updated: enlarged asserts `marker-mode="compact"` + scale 1.7; suite **565 pass**; typecheck/build clean. Playwright `:5189`, real 2026-06-08, enlarged at 1366×768: `.trade-cross` count **0**, 2 scaled ticks with titles ["Entry 09:30","Exit 16:00"], overflow 0px, 0 console errors (screenshot `analysis/replay_spread_diag/enlarged_compact_laptop.png`). Client-only → hard-refresh. |
| A177 | Grid-size replay charts are readable: entry/exit markers collapse to a **compact tick mode** (slim bottom tick + faint hairline per event) so the price action is unobstructed; the full arrows/rails/labels now live only in the enlarged (theater) view | GREEN | `MarketChart` gains `markerMode: "full" \| "compact"`; `renderEventMarkers` branches to new pure `layoutCompactEventTicks` (same cluster-dedup as the full markers — one tick per cluster, hover title preserved) + `createEventTick` (3px amber/rose bottom stub + 1px 0.22-opacity hairline, `.trade-tick*` in App.css). `ReplayCharts` passes `compact` to all three chart variants at grid size and `full` when enlarged. Validation: `layoutCompactEventTicks` unit test (dedup, sort, compact labels), `ReplayChartsEnlarge.test.tsx` +2 asserts (compact at grid / full+1.7 when enlarged, the non-enlarged sibling stays compact); suite **565 pass**; typecheck/build clean. Playwright `:5189`, real 2026-06-08, 1366×768: grid shows `.trade-tick`s and **zero** `.trade-cross` arrows; enlarging the SPX chart brings the full arrows back while the small spread tile keeps ticks; overflow 0px; 0 console errors. Screenshots `analysis/replay_spread_diag/compact_{grid,enlarged}_laptop.png` — the SPX tile is now a clean candle chart with entry/exit ticks along the bottom edge. Client-only → hard-refresh. |
| A176 | Daily Review shows a "Session & Execution" statistics panel directly under the entry/exit chart — day character + premium economics + risk/execution depth, non-duplicative with the metric cards and P/L overlay | GREEN | New pure `src/dailyReviewStats.ts#buildDailyReviewStatItems({trades, spxBars})` → up to 9 chips: SPX session move (O→C pts/%), range (H/L, % of close), **vol vs typical** (realized pts/√min ÷ CONE_PRIOR_A — bar-spacing-aware: the replay payload serves 5-SECOND bars, so the per-bar RMS normalizes by √(barSeconds/60); red tone ≥1.5×), profit factor (gross won/lost in detail), credit captured (net P/L ÷ Σ credit·contracts·100), avg credit, avg hold (+ longest, held-to-expiry count), **closest strike approach** (min distance from SPX to any open trade's short strike during its holding window — call vs highs, put vs lows; breach count; red when breached), entry slippage flags (entryChartDeviationFlag count). Rendered in `DailyReviewScreen` right after the `review-entry-exit-panel` section (`.review-stats-panel` + auto-fit `minmax(168px,1fr)` grid — cannot side-scroll); hidden when the date has no trades. `dailyReviewStats.test.ts` **10/10** (RMS rate, 5s-bar normalization to 2√12, put/call approach windows + breach, full-list values incl. +19% capture & 1h30m hold, ∞ profit factor, hot-session tone); suite **562 pass**; typecheck/build clean; Playwright `:5189` on real 2026-06-09 (the 245-pt range day): 9 chips, overflow 0px at 1366 + 1700, 0 console errors — and the chips read true (vol 1.97×, PF 0.34, capture −56%, approach 17.2 pts no breaches). Screenshots `analysis/replay_spread_diag/review_stats_*.png`. Client-only → hard-refresh. |
| A175 | Replay cockpit entries/exits are readable via a per-chart **enlarge (theater) mode** — bigger markers + printed labels — with NO horizontal scrolling at any viewport | GREEN | The SPX + spread chart panels each get a `⤢` button (`.chart-enlarge-btn` in the title tools): clicking lifts that panel into a fixed full-viewport overlay (`.chart-panel-enlarged`, `position: fixed; inset: 16px; z-index 70` over a click-to-close backdrop) — lightweight-charts `autoSize` + the marker layer's ResizeObserver re-fill the space automatically. Markers scale ×1.7 through the LAYOUT math (`layoutEventMarkers`/`markerLayoutCandidate`/`assignRailGroups`/`createEventArrowSvg` gained a scale factor; tip-anchoring invariant `markerX+tipX==anchorX` holds at any scale), and above scale 1.2 each marker PRINTS its label (`.trade-cross-label` — "Entry 09:30", "E1-3 11:17") beside the arrow instead of hiding it in a hover tooltip, flipping to the marker's left when it would clip the right edge. Esc, the ✕, or the backdrop restore the grid; one chart enlarges at a time. Validation: `MarketChart.test.ts` +1 (scaled geometry keeps tips anchored), new `ReplayChartsEnlarge.test.tsx` 2/2 (toggle/backdrop/Escape with a mocked chart), suite **553 pass**, typecheck/build clean; Playwright on `:5189` (built app, real 2026-06-08 data) at 1366×768 AND 1700×1000: 2 enlarge buttons, horizontal overflow **0 px in both normal and enlarged states**, Esc restores, 0 console errors; screenshots `analysis/replay_spread_diag/enlarged_{spx,spread}_{laptop,wide}.png` show full-screen charts with readable labeled markers. Client-only → hard-refresh. |
| A174 | Daily Replay's reconstructed put/call spread graphs are readable and truthful — no full-width sawtooth, no exact −width/0 pins from stale legs, no phantom post-close tail; spread marks regain a quote-midpoint source | GREEN | Diagnosis (2-agent sweep + live screenshots over Jun 4–9): every replay spread series was 100% TRADES-print based — the MIDPOINT-preferred waterfall in `daily_spx_ibkr_sync.py` was dead code because `option_leg_midpoints_5s.parquet` was only ever READ (line ~4150), never pulled: that sidecar was ThetaData's job, removed 2026-06-03 with no IBKR replacement. Sparse trade prints + independent per-leg forward-fill → fresh-print×stale-print pairings flipping the mark full-width (Jun-9 7400P: 98 jumps ≥$1, 114 min pinned at exactly −5.00; one 0.00→−5.00 step in 2 min) and OTM sides flat-lining at exactly 0; every series also carried 14 phantom forward-filled marks past 16:00. **Fixes:** (1) upstream (`daily_spx_ibkr_sync.py`, backup `Documents\rubicon_sync_fix_backup_20260610\daily_spx_ibkr_sync.pre_midpoint.py`): the option pull now requests MIDPOINT bars for the day's spread-leg contracts (one full-session request each, non-fatal, wall-clock-aware, merges + writes the sidecar parquet/csv; `midpoint_leg_row_count` in the summary) — spread marks prefer quote midpoints from tonight's sync onward; py_compile clean, IBKR pytest 29/29. (2) Rubicon `sanitizeSpreadMarksForTrades` (now exported): per-trade sequential pass drops marks after 16:00 ET and carries the last trusted CO-PRINT value over stale-leg trade-print marks that jump >0.25×width (baseline only advances on staleLegCount 0, so noise cannot staircase; quote/midpoint sources never touched; `rubicon_stale_leg_carry` source marker); `REPLAY_SAFE_STATE_VERSION` 3→4 rebuilds cached safe states. New `server/spreadMarkSanitize.test.ts` 7/7; archive test updated to the honest 391 marks (09:30–16:00); full suite 550 pass; typecheck clean. **Visual proof** (before/after clips in `analysis/replay_spread_diag/`): Jun-9 7385P/7400P sawtooth walls → coherent ITM declines ending −4.30/−3.90 (the old final marks −2.10/−0.30 were themselves artifacts; new values match the trades' realized −$1,002/−$928). Live server picks the serving-side fix up at its next restart (not restarted mid-RTH — would orphan today's auto-started feeds). |
| A173 | Daily IBKR sync survives server restarts, finishes the Google upload after a blocked-then-retried day, and stops starving the review-critical option pull | GREEN | Log forensics (3-agent sweep over `daily-sync-launch.log`, the IBKR python logs, and the Jun 1–9 summaries) confirmed three defects. **(1) Silent death:** the 2026-06-08 full sync logged 5 lines then vanished — `server/dailySync.ts` spawned the wrapper NON-detached with piped stdio, so the `npm run desktop` relaunch that evening (A123) killed it with the server; no exit marker, status stuck "running". Fix: detached spawn + fd stdio straight into the launch log + `unref()`; the wrapper owns its own completion status/lock so an orphaned sync still finishes and reports (close handler kept for the normal same-server case; `activeDailySync` type → `ChildProcess`). **(2) Blocked day never uploads:** the review gate throws before sheet-payload/google-upload, and the manual option retry blanket-marked them "complete" without running them — **2026-06-09 never reached the Google tracker**. Fix: extracted `Invoke-SheetPayloadAndUploadSteps` in the wrapper (called in the main flow AND in the retry branch after the sidecars, when the summary shows no prior upload and review is clear; honest warning marks when blockers remain). Verified live: ran the payload + upload for 2026-06-09 — 160 cells updated, Trade Log rows from 112, `uploadedAt 2026-06-10T12:55:59Z`. **(3) Starved option pull:** per-request timeout 15s truncated band-edge legs at ~70% of session bars (every day Jun 3–9) and the 360s wall-clock guillotined the spread-leg pull → zero spread marks → blocked review. Wrapper defaults now 25s / 480s. Wrapper parses clean (0 errors); `dailySyncWrapper.test.ts` 4/4 incl. a new assert that the retry branch runs the upload steps after the sidecars; full suite 543 pass; typecheck clean. Wrapper backup: `Documents\rubicon_sync_fix_backup_20260610`. |
| A172 | The Estimator's SPX expected-move cone is recalibrated for accuracy: measured intraday variance-time accrual (not linear √t), per-side calibrated bands for the implied scale (variance-risk-premium correction + crash skew), and an honest implied-scale path (ill-conditioned credits can no longer smuggle the stale 1.21 prior in as "implied") | GREEN | Phase-5 study (`AI STUFF/analysis/expected_move_cone/phase5_calibrate.py` + JOURNAL.md Phase 5; constants generated by its `export` step, not hand-tuned). **Findings:** (1) intraday variance is front-loaded — the old linear-√t cone was ~25% too narrow (variance) anchored at the open, ~15-25% too wide midday (z² 1.56/0.74 vs profile 0.98-1.20, oracle day-var, 771 days 2023-05→2026-06); (2) per-day option-implied scales dominate: chain-straddle Spearman 0.92 / FPL-credit-implied 0.88 vs realized remaining RV (first-30-RV 0.75, static prior 0.60) — validated on 582 days of full ThetaData 0DTE chains + 530 days of real FPL NBBO credits with the app's exact inversion (python↔TS parity 6 decimals incl. fallbacks); (3) VRP bias c=0.94 (credit) + crash skew quantified → per-side multipliers (dn .845/1.687/2.180, up .965/1.595/1.843 at levels 1/1.645/2). **OOS gate (2025-10→2026-06, fit ≤2025-09)**: credit-source 1.645σ coverage .897/.893/.875/.894 at +30/+60/+120/close with balanced ~5/5% breaches vs old .850/.854/.862/.895 down-heavy (dn 8.7% at +30); 2σ .931-.948 vs .887-.944. Prior fallback keeps symmetric RMS k (median-bias correction provably wrong for unconditional scales) + gains the profile shape (1.645σ intraday .866→.883-.896). **Impl:** `src/expectedMoveCone.ts` — `CONE_VARIANCE_CUM` (390-pt measured profile), `varianceShare` (linear fallback for non-390-min sessions), per-side `halfUp/halfDown` (chart consumes upper/lower unchanged), `coneScaleFromSpreads` passes `aPrior=NaN` so inversion fallbacks drop out; `priorRate`/`ConeScale` semantics preserved (thetaSpeedCurve untouched, 5/5). Tests 20/20 cone (+ multiplier pins, profile law, NaN-fallback, half-day linear fallback); full suite 539 pass; typecheck/build clean; browser smoke on `:5189` — Estimator renders the cone legend/readout with 0 console errors (screenshot `analysis/expected_move_cone/phase5_estimator_smoke.png`; band degenerate after-hours by design — live look next session). Adversarial verify: parity + stats-audit + code-review agents (see WORKLOG). |
| A162 | Daily sync runs the TC2000 export + daily-bars steps even when Data Collection blocks local review — a blocked option pull must not silently skip the independent Morning screener refresh | GREEN | Root cause of the 2026-06-06→09 staleness: `tc2000-daily-bars.json` froze at 06-05 because the Jun-8 full sync died after launch and the Jun-9 run aborted at the wrapper's "Data Collection blocked local review" throw (`daily-sync-launch.log` lines 750/762) — every step after the gate (incl. TC2000) was skipped, and the option retry marks them "Skipped". Fix: the wrapper's TC2000 block (open/export/qullamaggie-report/daily-bars) is extracted into `function Invoke-Tc2000Steps` (message vars `$script:`-scoped) and called BOTH in the normal flow and in the blocked-review branch before the throw (wrapped in try/catch so a TC2000 crash can't mask the real blocker). Also fixed the TC2000 cold-launch resolver in `AI STUFF/scripts/run_tc2000_qullamaggie_export.ps1`: adds the real per-user install candidates (`%APPDATA%\Worden Brothers, Inc\TC2000\`) and skips MSI icon-cache `\Microsoft\Installer\` DisplayIcon paths (the resolver would have launched `TC2000_1.exe`, not the app) — verified by an actual `-OpenIfClosed -EnsureOnly` cold launch picking `...\Worden Brothers, Inc\TC2000\TC2000.exe` (TC2000 left running for tomorrow's export). Both PS files parse clean (`Parser::ParseFile` 0 errors); `server/dailySyncWrapper.test.ts` 3/3 — the new case asserts `Invoke-Tc2000Steps` is invoked between the spread-leg retry and the blocked throw. NOTE: the wrapper + export script live OUTSIDE this repo (no git there); pre-edit backups at `C:\Users\charl\Documents\rubicon_r0_wrapper_backup_20260609`. |
| A163 | TC2000 daily-bars ingest reads only the `*_latest.csv` scanner exports (single-newest-CSV fallback; `--all-exports` restores the legacy union) and rejects non-ticker OCR junk, reporting it as `rejectedSymbols` | GREEN | `scripts/refresh-tc2000-daily-bars.py`: new `select_export_files` (default `*_latest.csv` only — a stale dated export can no longer drag garbage back into the union), `is_plausible_ticker` (`^[A-Z]{1,5}([.\-][A-Z]{1,2})?$` + blocklist NYSE/NASDAQ/PINK/OTC/headers — kills the 2026-06-03 `346P`/`NYSE`/`PINK` failure mode incl. PINK which resolves at IBKR), rejected tokens printed to stderr + written as `rejectedSymbols` in the payload. New stdlib-only `scripts/test_refresh_tc2000_daily_bars.py` **8/8** (importlib-loads the dashed module; covers accept/reject lists, latest-only selection vs a newer stale dated export, newest-fallback, `include_all`, junk filtering + rejected reporting). `py_compile` OK. Real refresh against TWS (client 947): fresh snapshot `generatedAt 2026-06-10T00:48Z`, 38/38 symbols with bars through **2026-06-09** (was stuck at 06-05), sources = the four `*_latest.csv` only, rejected []. |
| A164 | The Estimator's live SPX-bars sidecar uses IBKR client id **949** — 947 belongs to the TC2000 daily-bars pull, and IBKR allows one connection per client id (a daytime sync rerun while the feed held 947 would deadlock, the 2026-06-02 TC2000 crawl) | GREEN | `scripts/refresh-spx-live-bars.py` `--client-id` default 947→949 + docstring; `server/spxLiveBars.ts` `DEFAULT_CLIENT_ID` 947→949 (env `SPX_LIVE_BARS_CLIENT_ID` still overrides) + comment. Known id map now: holdings 884 / heatmap 941 / TC2000 947 / 0DTE chain 948 / SPX live bars 949. `npm run typecheck` clean; `server/spxLiveBars.test.ts` 3/3. Takes effect on the next feed start (weekday ~09:28 ET auto-start or manual). |
| A165 | GitHub Actions CI runs `npm ci` + `npm run typecheck` + `npm run test` on every push/PR (the AGENTS.md mandate) | GREEN | `.github/workflows/ci.yml` on **windows-latest** — Rubicon is Windows-only (WMI launcher, PowerShell toasts, Edge PWA) and the first ubuntu run failed on Windows path-shape assertions. Machine-local integration tests now `skipIf` their inputs are absent so CI is honest, not contorted: `dataImporter.test.ts` archive tests (real AI STUFF sessions) behind `hasLocalTradeArchive`, `dailySync.test.ts` preflight/source-health + `dailySyncWrapper.test.ts` behind the real wrapper's existence. Local full suite stays fully active (85 files / 525+ tests, nothing skipped on the trading machine). Proof: `gh run watch --exit-status` exit 0 on push `8dc97b4`. |
| A166 | The "Rubicon Server" scheduled task serves a FRESH dist — editing source and forgetting `npm run build` can no longer leave the installed PWA silently stale | GREEN | `scripts/serve-headless.vbs` now launches new `scripts/serve-headless.mjs`, which mirrors the desktop launcher's `ensureBuildIsFresh` (newest mtime of index.html/package*/public/server/shared/src vs `dist/index.html`), runs `npm run build` when stale/missing, then starts `tsx server/index.ts` detached (same lifecycle as before; `RUBICON_SKIP_HEADLESS_BUILD=1` escape hatch; build FAILURE logs to `data/serve-headless.log` and serves the existing dist so a broken build can't take Rubicon down at logon). Verified live: stale dist detected → rebuilt in 4.5s → duplicate server deferred to the running instance via the port probe (PID untouched). |
| A167 | `writeJsonAtomic` survives concurrent writers — unique temp file per write + Windows rename-contention retry | GREEN | `server/jsonStore.ts`: temp = `<target>.<pid>.<uuid>.tmp` (shared `${target}.tmp` let one writer's rename consume the other's payload → ENOENT), rename retries transient EPERM/EACCES/EBUSY ×10 with backoff (Windows locks the destination mid-replace; also covers antivirus/indexer touches), orphaned temps removed on failure. New `server/jsonStore.test.ts` 3/3 incl. 25 simultaneous writers to one target and a no-temp-leftovers sweep. |
| A168 | Journal snapshot saves MERGE by tradeId — a stale tab or empty browser profile can no longer wipe entries it never loaded | GREEN | `server/tradeJournalSnapshot.ts` adds `mergeTradeJournalSnapshot`: entries absent from the incoming payload survive; an entry only updates when the incoming copy is not older (`updatedAt`); sanitization unchanged. `PUT /api/journal-snapshot` now merges; `writeTradeJournalSnapshot` stays as the explicit full-replace maintenance primitive. `tradeJournalSnapshot.test.ts` 4/4: stale-client-missing-entry survival, no-rollback-to-older, newer-incoming-wins + sanitize-through-merge. |
| A169 | Daily-sync status cannot stick on "running" after a dead run | GREEN | `resolveDailySyncRuntimeState` treats persisted "running" with no live process and no ACTIVE lock as **failed** — covering a stale leftover lock AND the 2026-06-08 shape (wrapper died silently; its `finally` removed the lock; status said "running" forever). The review-readiness upgrade still reclassifies a finished-but-unreported run as completed, and the stale-message regex (`/running|waiting/` sniffing of user-facing text) is replaced by an explicit upgraded-to-completed flag. `dailySync.test.ts` updated + passing (suite green). `ok` stays a required boolean — every consumer already pairs it with `state === "failed"`, which now tells the truth. |
| A170 | Live-feed + launch logs rotate at 5 MB instead of growing forever | GREEN | New `server/logRotation.ts#openRotatingLogStream` (rotate current → `<log>.1`, replacing the previous archive, at stream-open time) used by all five writers: `dailySync.ts` (×2 sites), `fplLive.ts`, `spreadSpeedLive.ts`, `spxHeatmapLive.ts`, `spxLiveBars.ts` — the only `createWriteStream(..., {flags:"a"})` seams in server/. `logRotation.test.ts` 4/4 (fresh append + parent dirs, below-cap append, rotate-to-.1, archive replacement). |
| A171 | A render crash shows a recoverable error screen instead of a blank page | GREEN | New `src/components/AppErrorBoundary.tsx` (class boundary) mounted around `<App/>` in `src/main.tsx`: shows the error message, a Reload button, and a "Clear saved UI state & reload" button (localStorage.clear), and logs the component stack. Styles live in `src/index.css` (loaded by main.tsx) so the crash screen doesn't depend on the crashed tree's `App.css` import. `AppErrorBoundary.test.tsx` 2/2 (jsdom): transparent pass-through + crash screen with message/buttons. |

## Recent Deltas

| ID | Requirement | Status | Proof |
|---|---|---:|---|
| A161 | Heatmap timeframe selector adds **Gap** (the opening gap — open vs prior close) and **4H** (trailing 4-hour window) alongside Day/1H/30M/5M | GREEN | `src/heatmapWindow.ts`: `HEATMAP_TIMEFRAMES` gains `gap` (label "Gap", `gap:true`, cap 3, ordered first) + `4h` (240 min, cap 2.5); new pure `openingGapPct(series)` returns the first printed minute's % vs prior close (the overnight jump, fixed for the day); `timeframeDef` now falls back to Day regardless of order. `SpxHeatmapPanel.tsx` adds a `tileTfPct(tile,index,lastIndex,tf)` dispatcher (gap → `openingGapPct`, else the existing `tileWindowPctAt`) routed through all four % consumers (tile fill, frame aggregates, hover head, peers), plus `sigmaMinutes = tf.gap ? 0 : windowMinutes` so a Gap σ uses the daily scale; the selector auto-renders the two new buttons with a gap-aware tooltip. `heatmapWindow.test.ts` 10/10 (+3 `openingGapPct` cases + updated order/4h/gap-def asserts). `npm run typecheck` clean; suite 500 pass (1 unrelated `dateIssueBadges` red); `npm run build` OK; built + served `:5196` on live SPY → toggle reads Gap·Day·4H·1H·30M·5M; **Gap** shows the opening gaps (legend ±3%, index +0.98%; NVDA +2.43% / AVGO +3.89% gapped up, GOOGL −0.83% down), **4H** the trailing window (legend ±2.5%, index +0.00% — flat since the 09:30 open as we're <4h in, confirming the day's +1.00% was essentially all opening gap), each recolours, 0 console errors (screenshot). Gap is fixed across scrub; works for SPY + QQQ; σ on Gap is in daily-σ units. Client-only — hard-refresh. |

| A160 | The heatmap auto-updates on S&P 500 / Nasdaq-100 reconstitutions: a new constituent is detected, auto-placed under its correct sector, the universe/weights refresh, and a toast fires — no more manual "Other"-bucket edits | GREEN | New `scripts/reconcile-index-membership.py` (stdlib-only; imports the feed's `build_index_configs`/`write_json_atomic` via importlib) diffs fresh SSGA/Slickcharts membership vs snapshot `data/heatmap-membership.json`, auto-classifies ADDS by sector (GICS→Finviz crosswalk; industry left "Unclassified" per the user's "auto sector, manual industry" choice) into auto-overlay `data/heatmap-classification-auto.json`, records drops, appends a changelog, emits a per-index JSON line. Safety gate (per-index floor + 5% churn cap) refuses garbage fetches. `server/spxHeatmap.ts` adds `loadClassificationMerged` (overlay merged UNDER curated `finviz-classification.json` — **base always wins**) at the single classification seam → both SPX+QQQ loaders pick up new names. `server/indexReconcile.ts` (armed in `index.ts` beside the heatmap auto-start, ~09:15 ET weekdays) runs the reconcile, fires `showLiveUpdateDesktopToast` ("S&P 500: +FDXF (Industrials) −EPAM") and forces a fresh `--source yahoo --indexes spx,qqq` universe pull ONLY on a real, gate-passed change. `.gitignore` commits the overlay (seeded `{}`); snapshot/changelog stay ignored. `npm run typecheck` clean; new `server/indexReconcile.test.ts` 10/10 (planner toast/gate/empty/bootstrap/error + injected-dep wiring) + `spxHeatmap.test.ts` overlay-merge case (overlay-only name placed by sector; base wins on conflict) + suite 497 pass (1 unrelated `dateIssueBadges` red); `python -m py_compile` OK; `npm run build` OK. Functionally verified: simulated NVDA-add → overlay `{"Technology":{"Unclassified":["NVDA"]}}` + changelog; a 30-name drop → gate blocked, exit 20, nothing written; a real **live** dry-run fetched fresh SSGA (504) + Slickcharts (101) and bootstrapped clean. Requires a server restart to arm the daily run; `npm run index:reconcile` runs it manually. |
| A159 | Heatmap σ (IV-normalized) view works on every timeframe with window-scaled standard deviations, not just Day — surfacing which names are moving unusually *right now* (last 5/30/60 min), not only on the session | GREEN | Supersedes A158's Day-only σ. Intraday variance accrues ~linearly with time, so a w-minute move's expected 1σ is the daily 1σ × √(w/390) (390 RTH min/session) and the move's σ over that window is the daily σ × √(390/w). New `windowSigma(pct, iv, windowMinutes)` in `src/sigmaMove.ts` (reuses `sigmaMove`, ×√(390/w); windowMinutes 0 = Day = the daily σ unchanged) + `RTH_MINUTES`; +4 `sigmaMove.test.ts` cases (Day-equals-daily, √(390/w) scaling, a move equal to the window's own 1σ reads ±1σ, null-guards). `SpxHeatmapPanel.tsx` swaps the three σ shade sites (tile fill, sub-industry peers, hover σ row) to `windowSigma(…, windowMinutes)` and re-enables the σ button on all timeframes (reverted the off-Day disable + the σ→% force-reset); the ±2σ legend is unchanged since σ is already normalized (timeframe-independent). `npm run typecheck` clean; `sigmaMove.test.ts` 8/8 + suite 479 pass (1 unrelated `dateIssueBadges` red); `npm run build` OK; built + served `:5197` → on SPY (IV present, 501/503 tiles): enabling σ then switching Day→5M keeps σ active + enabled (was auto-disabled before), legend stays ±2σ, and the map recolours to the windowed σ — e.g. GOOGL −0.5σ (day) → +1.3σ (last 5 min), NVDA +0.7σ → +0.1σ — 0 console errors (Day-σ vs 5M-σ screenshots). σ still needs live IV (yahoo-only QQQ has none → σ stays disabled there). Client-only — hard-refresh. |
| A158 | Heatmap adds 5-min / 30-min / 1-hour trailing-window timeframes (alongside Day) so tiles show the % change over that window, not just on the day | GREEN | Frontend-only — the per-minute `pctByTime` series is each stock's % vs prior close, so the trailing-window move is `price[i]/price[i−w]−1 = (1+pᵢ/100)/(1+pⱼ/100)−1`, derived with no new data/feed/backend. New pure `src/heatmapWindow.ts` (`windowPct(now,start)` + `HEATMAP_TIMEFRAMES` Day/1H/30M/5M with shrinking colour caps 3/1.5/1/0.5%) + `heatmapWindow.test.ts` 7/7. `SpxHeatmapPanel.tsx` adds a `timeframe` state + a `tileWindowPctAt` wrapper (resolves both endpoints via the existing `tilePctAt` seam, clamps the window start forward to the tile's first print) and routes all four % consumers — tile fill, frame aggregates (index %/breadth/sector tints), hover head, sub-industry peers — through it; per-timeframe colour cap; a Day·1H·30M·5M segmented selector beside the %/σ toggle; legend rescales to the active cap. σ stays a Day-only lens (IV is a daily vol) — disabled off-Day. `src/components/SpxHeatmap.css` styles `.heatmap-timeframe-toggle`. `npm run typecheck` clean; `heatmapWindow.test.ts` 7/7 + suite 475 pass (1 unrelated `dateIssueBadges` red); `npm run build` OK; built + served `:5198` → browser confirms Day legend ±3% / index +0.91%, 5M legend ±0.5% / index +0.05% (σ disabled), 1H legend ±1.5% / index +0.13%, tiles recolour on each switch, 0 console errors (Day-vs-5M screenshots: NVDA +1.56% day → +0.23% last-5m). Client-only — hard-refresh; works for both SPY and QQQ. |
| A157 | Morning → Heatmap adds a QQQ (Nasdaq-100) market map alongside SPX with every feature, reusing the SPX data pull so a stock in both is fetched once | GREEN | One large-cap feed pulls the SPY∪QQQ union once and projects two payloads. `scripts/refresh-spx-heatmap.py` is now index-aware: new `load_universe_qqq` (Slickcharts Nasdaq-100 weights → Wikipedia fallback), an index registry, `--indexes spx,qqq`, `pull_bars` (fetch each symbol once) + `build_index_payload` (project onto each index's members/weights/label) → writes `data/spx-heatmap.json` + `data/qqq-heatmap.json`; the live loop's snapshot / rolling-IV / earnings / forward-fill all run once over the union. `server/spxHeatmap.ts` factors a parameterized `loadHeatmapPayload` (reusing the dual-class fold, Finviz classification, forward-fill, sector derivation) + `loadQqqHeatmap`; `server/index.ts` adds `/api/qqq-heatmap` (+ `/live/*` proxied to the shared `spxHeatmapLive`, since one process writes both files); `spxHeatmapLive` launches `--indexes spx,qqq` (one IBKR client id). 13 Nasdaq-only names (ARM, ASML, MELI, PDD, SHOP, MSTR, ZS …) added to `data/finviz-classification.json`. Frontend reuses `SpxHeatmapPanel` via an `index` prop (generic `fetchHeatmap`/`startHeatmapLive` in `src/api.ts`) with an SPY|QQQ toggle in `MorningDashboard`. Reuse proven: union = 517 symbols (504 SPX + 13 QQQ-only), AAPL's bars identical across both, weights differ (SPX 7.0% vs QQQ 11.7%). `npm run typecheck` clean; `server/spxHeatmap.test.ts` 11/11 (+2 QQQ loader cases) + suite 459 pass (1 unrelated `dateIssueBadges` red); `python -m py_compile` OK; built + served `:5199` → browser toggle SPY↔QQQ renders "Nasdaq-100 (QQQ weights)" with ~100 tiles, GOOGL dual-class merged, sector→industry→stock nesting + hover, 0 console errors (screenshot). Needs a server restart + hard-refresh for the new routes/bundle; live QQQ data lands on the next feed start. |
| A156 | The Estimator overlays a forward SPX expected-move CONE on the 2-min intraday chart (±1σ / ±1.645σ "0.05Δ frontier" / ±2σ from spot to 16:00 ET), validated on 745 historical SPX days before building | GREEN | **Phase 0 (gate) — backtest** `AI STUFF/analysis/expected_move_cone/backtest_cone.py` over 745 RTH days (2023-05→2026-05, 1-min closes): the √t variance law holds (R²=0.991); fat tails are extreme (excess kurtosis ≈39 + downside skew) and daily vol varies hugely (per-day realized scale 0.5→5+), so a flat prior mis-calibrates BUT per-day scaling fixes it — standardizing by the early-session realized vol (proxy for the live implied scale) pulls 1.645σ/2σ coverage to ~91%/95% (≈nominal), and the oracle daily scale makes the move ~Gaussian (kurtosis 35→4.8). Also found the app's `A_PRIOR=1.21` is stale for this regime (measured ≈2.0–2.4). Verdict (`JOURNAL.md`): the cone is sound when scaled to the day's vol — which the estimator already does via `impliedScale`. **Build** — new pure `src/expectedMoveCone.ts` (`expectedMoveCone` = k·r·√elapsed √t band; `coneScaleFromSpreads` = median of the live spreads' `impliedScale`, same vol as the P/L curve, with a per-time-of-day prior fallback from the backtest, NOT the stale 1.21) + `expectedMoveCone.test.ts` 10/10. `EstimatorSpxChart.tsx` draws 6 paired lightweight-charts v5 line series (±1σ sky / ±1.645σ amber dashed = 0.05Δ frontier / ±2σ slate), created once + `setData` in place on a forward 120s epoch grid that extends the axis to 16:00; `LiveSpreadEstimatorPanel.tsx` derives the cone (replay-aware anchor, on/off toggle + legend tying 1.645σ to the 0.05Δ short strike). `npm run typecheck` clean; cone+panel 12/12 + suite 448 pass (1 unrelated `dateIssueBadges` red); `npm run build` OK; isolated lightweight-charts harness screenshot confirms the forward cone renders (pinches at spot, widens to close, axis extends past the last candle); estimator screen loads with zero console errors. Client-only — hard-refresh; uses live implied vol when spreads are on screen (market hours), else the typical-day prior. |
| A155 | SPX heatmap no longer blanks the whole map grey for one minute every ~11 min (and the formerly-skipped minute now carries real data) | GREEN | Root cause (confirmed in data + live log): the IBKR live loop's per-stock IV sweep (~48s, generic tick 106 over ~503 names) plus the ~36s price snapshot overran the 60s minute boundary, so the boundary-sleep skipped a whole minute index — every tile's `pctByTime[idx]` stayed null → a grey "blank minute" (`data/spx-heatmap.json` held 13 such 503/503-null interior minutes; `spx-heatmap-live.log` showed the counter jumping `11:10 → 11:12`). Two fixes: (1) loader — new pure exported `forwardFillTileSeries` in `server/spxHeatmap.ts` computes the global frontier and carries each tile's last non-null `pctByTime` forward across *interior* nulls (first-print → frontier; leading/trailing nulls untouched), wired into `loadSpxHeatmap`, so a skipped minute renders as the prior minute's colours instead of grey and today's already-written blanks are fixed on serve; (2) root cause — `scripts/refresh-spx-heatmap.py` now sweeps IV in a per-minute rolling slice (`iv_cursor` walks `iv_slice=64` names, full universe every ~8 min) instead of one ~48s burst every 10 min, so each iteration stays < 60s and no minute is skipped. `npm run typecheck` clean; `server/spxHeatmap.test.ts` 9/9 (5 new `forwardFillTileSeries` cases + updated loader frontier assertion); running the patched loader over the real `data/spx-heatmap.json` → 0 all-null interior minutes (was 13); `python -m py_compile` OK; full suite 431 pass (2 unrelated reds — `dateIssueBadges` + `dailySync`, concurrent daily-pull WIP). Server-side only (loader + Python) — no client bundle change; requires a server restart for the loader fix, and the rolling sweep activates on the next feed start. |
| A154 | When multiple calendar events share the same start time, Rubicon fires ONE coalesced notification listing them all (toast + in-app card + a single sound), instead of one notification per event | GREEN | New pure `calendarAlertGroups`/`nextCalendarAlertGroup` in `src/calendarAlerts.ts` group per-event `calendarAlertTargets` by identical `eventAt` into a `CalendarAlertGroup { eventAt, events[], millisUntilAlert }`; `formatCalendarAlertStatus` now takes a group and renders "8:30 AM - 3 events" for clusters. `MorningDashboard.tsx` schedules ONE timer per group (marks every event id in the group notified on fire), `fireCalendarAlert(group)` plays the sound once + sets a `{ events[] }` popup, and `showWindowsCalendarAlert(events[])` builds a single toast ("3 calendar events start in 1 minute" / body "CPI • Jobless Claims • Retail Sales"); `CalendarAlertOverlay` lists every event (new `.calendar-alert-list` in `App.css`). `npm run typecheck` clean; `calendarAlerts.test.ts` 7/7 (incl. new coalesce + count-summary cases) + suite 427 pass (1 unrelated `dateIssueBadges` red); `npm run build` OK; live-verified on `:5191` — one toast rendered all three 8:30 events as a single Action-Center entry (same Rubicon AppId). Client+server bundle change → hard-refresh; restart the server to serve the rebuilt bundle. |
| A153 | Estimator places the P/L-vs-SPX curve and the SPX 2-min intraday chart side by side, with the target-level slider spanning below both | GREEN | `src/components/LiveSpreadEstimatorPanel.tsx` wraps `AggregateChart` (the P/L curve) and the `EstimatorSpxChart` block (SPX candles + LIVE/replay badge + Start/Stop feed button) in a flex row — `flex:1 1 340px` each, `flex-wrap` so they stack on narrow widths, `align-items:flex-start` — and the "target level" slider + quick buttons moved below the row since they drive both charts. Each chart gets a small heading ("portfolio/spread P/L vs SPX level" left, "SPX intraday · 2m" right). `npm run typecheck` clean; `LiveSpreadEstimatorPanel.test.tsx` 2/2; `npm run build` OK; served `:5192` and screenshotted the Estimator at 1500px — bounding boxes confirm the two charts side by side (P/L x 41–744 ×230h, SPX x 757–1393), the slider full-width below, zero console errors. Client-only (JSX) — hard-refresh, no server restart. |
| A152 | Morning Brief stacks Calendar + AI Notes as one left column so AI Notes sits directly under the Calendar, matching the Live Updates column height | GREEN | `src/components/MorningDashboard.tsx` wraps the Calendar (`MorningAgendaSection`) + AI Notes panel in a `.morning-brief-stack` div; `src/App.css` makes it `display:contents` by default and, in the 761–2200px rule, a `flex-direction:column` left column (`grid-column:1/span 3`) with Live Updates filling the right and IBKR + TC2000 on the bottom row. AI Notes now sits snug under the Calendar (12px gap) instead of ~138px adrift. typecheck clean; `MorningDashboard.test.tsx` 8/8 + suite 417 pass (2 unrelated reds — `dateIssueBadges` + `dailySync`, concurrent daily-pull WIP); build OK; screenshot at 1680px confirms the snug stack. Client-only — hard-refresh, no server restart. |
| A151 | Calendar 1-minute desktop alert is now a native Windows toast (bottom-right / Action Center) like FirstSquawk, replacing the centered `wscript` `shell.Popup` dialog and the duplicate browser `Notification` | GREEN | `server/desktopAlert.ts` extracts the proven `spawnSync` toast block into a shared `launchWindowsToast(sanitized, appRoot, duration)`; `showCalendarDesktopAlert` calls it with `"long"` and `showLiveUpdateDesktopToast` with `"short"`, both funnelling through `scripts/show-windows-toast.ps1` + the same AppId resolution. `MorningDashboard.tsx` drops the calendar browser `Notification` (+ both now-unused helpers); the in-app `CalendarAlertOverlay` + sound stay, and `App.css` moves that card to the bottom-right corner. `scripts/show-calendar-alert.vbs` deleted. `desktopAlert.test.ts` now asserts the calendar toast spawn (`show-windows-toast.ps1` + `-Duration long`, no `spawn`). typecheck clean; suite 418 pass (1 unrelated `dateIssueBadges` red); build OK. Live-verified on `:5186` (bottom-right toast, same AppId as live-updates). Requires a server restart. |
| A150 | Live SPX intraday bar feed — the Estimator's 2-min chart shows live SPX during the session (not just post-close) via a dedicated IBKR sidecar | GREEN | New `scripts/refresh-spx-live-bars.py` connects once to TWS (own client id 947), re-pulls today's RTH 1-min SPX index bars (`Index('SPX','CBOE')`, TRADES, useRTH, today-filtered) every ~15s and atomically writes `data/spx-live-bars.json`; stops at 16:00 ET. `server/spxLiveBars.ts` adds the loader + a process manager (start/stop/status/auto-start ~09:28 ET, RTH-gated) mirroring `spxHeatmapLive`; `server/index.ts` exposes `/api/spx-live-bars` (+ `/live/status|start|stop`) and arms auto-start at boot; `SpxLiveBarsPayload`/`SpxLiveBarsLiveStatus` types added. `MorningDashboard` polls the feed while on the Estimator screen and passes the live bars (preferred over replay) plus a Start/Stop control; `LiveSpreadEstimatorPanel`/`EstimatorSpxChart` show a LIVE/replay badge + the feed button (chart created once, `setData`/`applyOptions` on updates so no flicker). `npm run typecheck` clean; new `server/spxLiveBars.test.ts` 3/3 + suite 419 pass (1 unrelated `dateIssueBadges` red); `python -m py_compile` OK; `npm run build` OK. **Live-verified against TWS** (weekday ~14:2x ET): the sidecar pulled 293 real SPX bars (09:30 7529.54 → 14:22 7593.26), `/api/spx-live-bars` served 294, and the Estimator 2-min chart rendered today's candles with the red Target price line tracking the slider (screenshot). Requires a Rubicon **server restart** to pick up the new routes + auto-start. |
| A148 | Estimator shows today's open AND closed 0DTE SPX spreads as clickable chips, with a single-spread focus mode (View Portfolio button) and a 2-min SPX intraday chart whose horizontal price line tracks the target-level slider | GREEN | `src/spreadEstimator.ts` adds `liveSpreadFromTradeRecord` (frames creditNow at entry), `todayClosedSpxSpreads` (filters tracker trades by date + SPXW + 0DTE + Credit + exited + non-Mixed; carries exit time + realised P/L), `activeSpreadsForResponse` (closed NEVER enter the portfolio aggregate; 1-element array routes through the same `buildPortfolioResponse` path as the aggregate), plus the `EstimatorSpreadOption` shape. +9 unit tests. `MorningDashboard` threads `trades` + `spxBars` into the panel; `App.tsx` passes `snapshot.trades`. `LiveSpreadEstimatorPanel` renders a horizontal chip rail (open solid + closed dashed/muted with realised P/L), a `← View Portfolio` button when focused, portfolio/focus modes, and a new `EstimatorSpxChart`. `npm run typecheck` clean; new Vitest 9/9 + panel 2/2 + prior pass (1 unrelated `dateIssueBadges` red); `npm run build` OK. (The "live SPX bar feed" follow-up is now done — see A150.) |
| A149 | SPX Heatmap adds a togglable "earnings" overlay — names reporting in the next ~2 weeks get a light-blue outline that intensifies as the last-tradeable date nears; before-open counts as the prior trading day | GREEN | New pure `src/earningsOverlay.ts#earningsHighlight` (BMO → previous trading day; in-window = within ~2 weeks / 10 trading days and ≥ today; intensity floors ~0.45 and ramps to 1 at 0 days; +8 tests incl. "next Wednesday when today is Wednesday"). New `scripts/earnings_nasdaq.py` (free Nasdaq calendar — every company per date, ~5 weekday HTTP calls for the whole 500, no IBKR/key) is fetched by `refresh-spx-heatmap.py` at live-loop startup and writes `earningsDate`/`earningsTime` onto each tile; `SpxHeatmapTile` gains both; loader threads them through the dual-class merge. `SpxHeatmapPanel.tsx` adds an "Earnings" toggle drawing a blue outline+tint per this-week tile (strokeWidth/opacity scale with intensity), independent of the %/σ mode, plus a tooltip earnings line; `SpxHeatmap.css` styles it. `npm run typecheck` clean; new Vitest (earningsOverlay 7/7 + loader earnings passthrough) and the suite's 415 pass (1 unrelated `dateIssueBadges` red); `python -m py_compile` OK; `npm run build` OK; served `:5185` with synthetic earnings → toggle draws 19 blue outlines, NVDA (today) brightest/thickest, AAPL/AVGO (further out) dimmer, non-earnings tiles none (2× screenshot verified). |
| A147 | SPX Heatmap adds a % / σ toggle that recolours tiles by the IV-normalized move (standard deviations) using each stock's own ~30-day ATM IV | GREEN | New pure `src/sigmaMove.ts` (σ = move% ÷ (IV/√252); +4 tests). `scripts/refresh-spx-heatmap.py` sweeps IBKR per-stock IV (generic tick 106, streaming) every ~10 min inside the live loop and writes `iv` onto each tile; `SpxHeatmapTile.iv` added; loader threads it through (dual-class merge keeps the primary's IV). `src/components/SpxHeatmapPanel.tsx` adds the "% / σ" toggle (colours via `heatmapColor(σ, cap 2)`, σ tile labels, legend → ±2σ, IV+σ in the tooltip; auto-falls-back to % when no IV). Step-0 spike confirmed tick-106 IV for 16/16 sampled names vs live TWS. `npm run typecheck` clean; new Vitest (sigmaMove 4/4 + loader iv passthrough/merge) and 402 prior pass (1 unrelated `dateIssueBadges` red); `npm run build` OK; served `:5185` with synthetic IV → σ view recolours (AVGO −7.4σ, JPM +2.4σ, LLY +3.7σ), legend reads ±2σ, tooltip shows IV+σ (screenshot verified). |
| A146 | SPX Heatmap hover shows a Finviz-style sub-industry panel — every stock in the hovered tile's sector+industry with its daily % change | GREEN | New pure `src/heatmapPeers.ts#industryPeers(tiles, sector, industry)` (filter by sector+industry, sort by weight; +3 tests) feeds a rebuilt hover tooltip in `src/components/SpxHeatmapPanel.tsx`: the hovered stock on top, then every sub-industry peer as a colour-swatch + ticker + last + signed % row (heaviest first), with edge-aware positioning + max-height clamp; styles in `src/components/SpxHeatmap.css`. `npm run typecheck` clean; focused Vitest 10/10; `npm run build` OK; served `:5185`, hovered NVDA → "SEMICONDUCTORS · 13" with all 13 names + % changes (2× screenshot verified). 1 unrelated `dateIssueBadges` red (concurrent daily-pull WIP). |
| A145 | Estimator auto-refreshes live 0DTE SPX spreads every 5 min on weekdays from 09:30 ET, with a LIVE / Stale / Pre-market / Closed indicator | GREEN | Server already pulls holdings every 5 min on weekdays 09:30–16:15 ET (`armIbkrHoldingsAutoRefresh`); added the missing client side. New pure `src/estimatorLiveState.ts` — LIVE only when in the weekday window, viewing today, and `fetchedAt` is fresh (≤ ~2× the 5 min interval); STALE / PRE_MARKET / CLOSED otherwise; badge greys at 16:00 even though pulls run to 16:15 (+9 unit tests). `MorningDashboard` adds a visibility-gated 60 s read-only poll of `/api/ibkr-holdings` (never POSTs /refresh → no competing TWS connection) that runs only while viewing today inside the window, plus a 30 s heartbeat. `LiveSpreadEstimatorPanel` shows a pulsing green "LIVE · auto every 5m · updated HH:MM:SS" pill (amber Stale / grey Closed) via `.estimator-live-dot` CSS. `npm run typecheck` clean; new Vitest 9/9 and 397 prior pass (1 unrelated `dateIssueBadges` red from concurrent daily-pull WIP); `vite build` OK. |
| A144 | Morning SPX Heatmap nests sector → industry → stock on the Finviz taxonomy, with dual-class holdings merged into one tile | GREEN | New `data/finviz-classification.json` (500 S&P 500 names, 11 Finviz sectors, transcribed from Finviz's map and reconciled vs the live SPY-holdings universe). `server/spxHeatmap.ts` folds GOOG→GOOGL / FOX→FOXA / NWS→NWSA into one weight-summed, %-blended tile (503→500) via `mergeDualClassTiles`, overlays Finviz sector+industry per tile via `applyClassification`, and re-derives sector aggregates via `computeSectors`; `SpxHeatmapTile` gains `industry`; `src/components/SpxHeatmapPanel.tsx` renders a 3-level squarified treemap (sector → industry sub-block/caption → stock). `npm run typecheck` clean, Vitest 377/377 (incl. 2 new loader tests), `npm run build` passed; served `:5183` → `/api/spx-heatmap` returns 500 Finviz-sectored tiles and the treemap renders sector→industry→stock with GOOGL as one merged tile. |
| A143 | Morning > Estimator centers on live IBKR 0DTE SPX spreads with a per-spread move and an aggregate portfolio response | GREEN | New `src/spreadEstimator.ts` selects 0DTE SPXW verticals from the live holdings pull; `src/portfolioResponse.ts` runs each through the existing Bachelier model on a shared SPX ladder and sums an aggregate P/L curve; `src/components/LiveSpreadEstimatorPanel.tsx` shows live spreads + aggregate as primary with the custom what-if demoted to a collapsed disclosure; `server/ibkrHoldings.ts` adds a 5-minute market-hours live pull. Full Vitest 375/375 and `vite build` passed; typecheck clean for new code. |
| A142 | Morning macro calendar pulls/generates public-source timing for previously rated rows | GREEN | US macro calendar now emits MBA, ADP weekly/monthly, API crude, NAR existing-home sales, UMich preliminary sentiment, NY Empire State, and NAHB HMI timing events. Macro/Morning focused tests, typecheck, and build passed. |
| A141 | FirstSquawk word-filter notifications use native Windows toast notifications | GREEN | Live update alert payloads now call `/api/desktop-alert/live-update`, which launches `scripts/show-windows-toast.ps1` through the existing `Rubicon.RubiconApp` AppUserModelID. Focused tests, PowerShell parser/script smoke, typecheck, build, browser smoke, and API smoke passed. |
| A140 | Daily Pull separates Data Collection, Rubicon Ingest, and Google Upload pipeline stages | GREEN | Status now carries run id, target date, stage rows, review-ready and Google-uploaded verdicts, cross-process lock state, and catch-up state. The wrapper runs Data Collection -> Rubicon Ingest -> Google Upload, with Google failures non-blocking for local review. Focused/full tests, typecheck, build, IBKR pytest, and syntax checks passed. |
| A139 | Morning hides event subheaders and routine Godel, AI Notes, and TC2000 status text | GREEN | Calendar event rows no longer render metadata subheaders, the old Godel setup paragraph is filtered/shortened, AI Notes hides generated empty-state message text, and TC2000 hides the latest-pulls subtitle plus scanner-list count. Focused tests, typecheck, build, and browser smoke passed. |
| A138 | Rubicon shortcuts and app metadata keep the Rubicon icon instead of Edge | GREEN | Desktop and Start Menu shortcuts now use generated `public\favicon.ico`, the launcher/build path ensures the ICO exists, and the app advertises `/favicon.ico` for Edge app-mode. Focused icon tests, shortcut reinstall/inspection, typecheck, and build passed. |
| A137 | Replay and Daily Pull hide routine count/status narration while preserving actionable failure state | GREEN | Replay now avoids pending-today explanation, "full day" wording, trade-count-in-view copy, and chart raw counts; Daily Pull uses terse date/status copy and hides successful source/readiness chatter. Focused tests, typecheck, build, and browser smoke passed. |
| A136 | Calendar notifications work at the computer level, not only inside the app | GREEN | Backend calendar alerts now launch `wscript.exe` with an auto-closing Windows Script Host popup. Focused desktop-alert tests, typecheck, build, live API trigger, and screenshot proof passed. |
| A135 | Morning dashboard hides routine source/success/readiness text while keeping failures visible | GREEN | Morning now shows a date-only header, hides ok source pills/details, strips calendar source metadata and high-importance time/source metadata, uses source-agnostic Live Updates status, suppresses successful IBKR refresh messages, removes AI-note ready copy, and drops TC2000 hit/daily-bar readiness counters. Focused tests, typecheck, build, and browser smoke passed. |
| A134 | Pull Dates can suppress accepted issue-count badges without hiding diagnostics | GREEN | Daily Pull date rows now show a separate `Issues fine` button when a date has a visible issue badge. Accepted dates persist in localStorage and remove only the Pull Dates badge/count; detailed diagnostics remain visible. The Pull Dates rail is wider on desktop and stays before the main panel on narrow screens. Typecheck and build passed. |
| A133 | Old visible-screen Godel watcher is deleted and Morning uses only the minimized-safe DOM bridge | GREEN | Removed watcher server module, Python watcher/test scripts, watcher npm command, shared watcher types, API helpers, Express routes, and Morning Start/Stop watcher controls. Bridge status/setup remains available; focused tests, typecheck, build, stale-reference sweep, and temporary API smoke passed. |
| A132 | TC2000 scanner list highlights stocks that are new versus the prior saved scanner state | GREEN | Morning payloads now carry optional `newSymbols` metadata per TC2000 scanner list, computed from the most recent prior saved Morning state. Scanner stock buttons get a `new` class/accessible label/title and a warm highlight/dot; focused tests, typecheck, build, and browser smoke passed. |
| A131 | Spread Speed uses sidecar-only saved state and cannot reopen the giant upload payload | GREEN | `loadSpreadSpeed()` now reads/rebuilds `rubicon_spread_speed_state.json` through `loadSafeSpxBars()` and option-leg sidecar CSV candidates, with stat-only cache validation and one frame per minute. Daily sync completion refreshes this state with tracker, Replay, and Morning derived state. `/api/spread-speed` no longer forwards `full=1`, and stale full-mode callers stay on the safe-state path. Focused tests, typecheck, build, desktop relaunch, heavy-date timing probes, and live API probes passed. |
| A130 | Explicit Replay uses a per-date safe replay state by default | GREEN | `rubicon_replay_safe_state.json` is written beside each date archive and default `/api/replay` reads it before raw artifacts. Daily sync completion refreshes this safe state with tracker and Morning derived state. Safe Replay avoids the 449MB sheet payload, keeps SPX bars, quick-trade spread marks, OI, and five-minute volume profile rows, while `/api/replay?full=1` remains available for raw-detail audit. Focused importer/sync/summary tests, typecheck, build, desktop relaunch, and heavy-date timing probes passed. |
| A129 | Daily sync completion refreshes derived state and Morning auto-refreshes after the 8:30 ET data window | GREEN | Sync completion now refreshes both `rubicon_tracker_summary.json` and saved Morning state for the completed summary date. App status polling refreshes tracker state once when it observes a new completed sync. Morning auto-refreshes saved state once per ET date at or after 8:30 when showing today's date. Targeted tests, affected Morning/server tests, full Vitest, typecheck, build, desktop relaunch, and live health/status probes passed. |
| A128 | Morning startup avoids row-level Replay/Spread Speed hydration | GREEN | `/api/tracker` now marks replay readiness from compact daily summary fields instead of reading SPX rows from the 449MB `google_sheet_upload_payload.json`, and `App.tsx` fetches `/api/replay` or selected-date `/api/spread-speed` only when Replay/Daily Review views need them. Focused importer/summary tests, typecheck, build, desktop relaunch, one-off timing/memory probes, and live `/api/tracker` probes passed. |
| A127 | Morning brief reads saved per-date state on normal loads and refreshes live sources only on explicit refresh | GREEN | `data\morning-brief-state\YYYY-MM-DD.json` stores the dashboard-ready Morning payload. Normal `/api/morning?date=...` reads saved state; `/api/morning?date=...&refresh=1` pulls live DailyFX/RollCall/TC2000 inputs and rewrites state. The Morning refresh button sends `refresh=1`, while initial loads/polls stay on saved state. Focused Morning-state tests, Morning server/dashboard tests, full Vitest, typecheck, build, desktop relaunch, and live API probes passed. |
| A126 | Tracker dashboard reads compact validated daily summaries instead of giant row-level artifacts | GREEN | `rubicon_tracker_summary.json` is written beside each daily pull folder, `/api/tracker` prefers those compact summaries, coalesces/caches immediate tracker reads, replay/detail still loads chart marks on demand, daily sync refreshes the compact summary after completion, and Google connector rows can confirm upload receipts without downgrading local IBKR counts/statuses. Focused backend tests passed 3 files / 40 tests; typecheck/build passed; one-off loader proof returned June 1 with option status `partial`, 540,996 option/volume rows, and about 1.2s load time. |
| A125 | Godel bottom-right blips can be captured while the Godel window is minimized | GREEN | Live Updates now shows a minimized-safe Godel DOM bridge setup/status strip. The setup page provides a bookmarklet that arms a MutationObserver inside the Godel page, watches bottom-right/toast-like DOM changes, posts headline-like blips back to Rubicon, and rejects numeric ladder rows. Focused bridge/Morning/Godel tests, typecheck, build, setup/status/ingest API probes, and live-update API probe passed. |
| A124 | Godel alert watcher can be started/stopped from Morning and rejects numeric false positives | GREEN | Live Updates shows a visible-screen Godel watcher strip with Start/Stop/status. The watcher calibrates from a visible `v4.4.9`-style Godel chat anchor to the right edge above the bottom bar, reports `anchor-not-found` when Godel is not visible, and rejects mostly numeric ladder rows. Focused Python/test checks, Morning/Godel Vitest files, typecheck, build, desktop relaunch, and API probes passed. |
| A123 | Desktop launcher restarts stale Rubicon backends before opening | GREEN | `npm run desktop` now rebuilds stale `dist`, restarts existing Rubicon servers by health PID, cleans up detached non-watch `server/index.ts` Rubicon processes including stale ports like `5187`, and starts the API with a configurable 16GB default heap. Health proof returned PID/start/appRoot, and the June 8 Morning probe returned `inflationExpectationsCount=0`. Syntax check, typecheck, and build passed. |
| A122 | Daily Review counts regular exits by closing action side | GREEN | CCS exits now map to Long/Put action side and PCS exits map to Short/Call action side in `buildDailyReview()` and `buildReviewMarkers()`. Timeline pills show Long/Short, the chart legend explains Long = PCS entries / CCS exits and Short = CCS entries / PCS exits, focused `src/stats.test.ts` passed 24 tests, typecheck passed, and build passed. |
| A121 | Morning high-importance events match live DailyFX/IG rows and preserve source names | GREEN | Live DailyFX/IG probe for `2026-06-01` through `2026-06-15` returned only `importance: 3` U.S. rows; direct `loadMorningBrief("2026-06-02")` matched those rows with no extras. Visible titles now preserve exact source names for single-row events and source-name clusters for simultaneous rows. Focused tests, typecheck, and build passed. |
| A120 | Morning Calendar splits today's agenda left and High-only major events right | GREEN | `MorningAgendaSection` renders today's DailyFX/RollCall agenda first and `Major events` second; desktop CSS splits into equal columns and mobile stacks. `parseIgMajorCalendarEvents()` keeps only DailyFX/IG `importance >= 3` rows while preserving monthly OPEX. Focused tests passed; Browser proof at `http://[::1]:5174/?qa=calendar-split-high-only` showed left/right columns, no medium major text, clean console, and no mobile overflow. |

## Core Loop Summary

Rubicon is GREEN for the local MVP loop:

Trader opens Rubicon -> Morning shows macro/live/model prep -> Replay imports local SPX tracker data -> trader reviews P/L and session health -> trader selects a trade -> replay charts advance through the day -> journal/review state persists locally.

## Deferred

| ID | Reason |
|---|---|
| A13 | Separate admin/operator view is outside the local MVP. |
| A20 | Full AI feature fallback remains deferred unless made core product scope. |

## Evidence Standard

Use the narrowest proof first:

1. Focused unit tests for logic.
2. `npm run typecheck` for TypeScript.
3. `npm run build` for shipped bundle proof.
4. Browser/API smoke only when UI, layout, or runtime wiring changed.

Full historical proof lives in `WORKLOG.md`; do not paste old acceptance rows back into this file.
